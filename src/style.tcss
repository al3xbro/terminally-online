.message {
    height: auto;
}

.content {
    width: 0.75fr;
}

.message-content {
    width: 0.75fr;
    height: auto;
}

.username {
    height: 1;
    width: 20;
}

.timestamp {
    width: 0.25fr;
    text-align: right;
    color: $primary-background;
}

.deleted {
    color: #ff4a4a;
}

Chat {
    padding: 2;
    padding-right: 0;
}

CommandInput {
   margin-left: 2;
   margin-right: 2;
   margin-bottom: 2;
}